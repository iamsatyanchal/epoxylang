$ Test error logging syntax

$ Test 1: error with string literal
error "this is an error";

$ Test 2: snafu with string literal  
snafu "this is also an error";

$ Test 3: error with variable
assign ermsg = "this is error from variable";
error ermsg;

$ Test 4: snafu with variable
assign msg = "snafu message";
snafu msg;

$ Test 5: error with expression
assign x = 10;
assign y = 20;
error x + y;

$ Test 6: Compare with show
show "this is a normal log";
error "this is an error log";
snafu "this is a snafu log";
